# 로컬 에이전트/도구 상태 (커밋 안 함)
.omc/

# Obsidian 로컬 작업공간 상태 (머신별)
.obsidian/workspace.json

# JetBrains IDE 로컬 설정
.idea/

# Quartz (정적 사이트 생성기)
node_modules/
public/
.quartz-cache/
# 주의: content/ 는 .gitignore에 넣지 않는다. Quartz가 .gitignore를 존중해
# staged content를 통째로 무시하기 때문. 대신 .git/info/exclude 로 로컬 추적만 제외한다.

